`use strict`

let students = ['Aguilera', 'Bermello', 'Caballero', 'Calle', 'Calle', 'Carriel', 'Correa', 'Cuesta'];

for (let i = 0; i < students.length; i++) {
  console.log(`${i + 1} ${students[i]}`);
}
